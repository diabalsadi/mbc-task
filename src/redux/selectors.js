export const getArticles = (state) => state.articles;
export const getModalMessage = (state) => state.modal.message;
export const getPageNumber = (state) => state.articles.page;
export const getTheme = (state) => state.themes.theme;
export const isModalOpen = (state) => state.modal.isOpen;